import { env } from '../../../env';
import { CityEmail } from '../../../components/CityEmail';
import { Organizers } from '../../../components/Orginizers';
import { FAQ } from '../../../components/FAQ';
import { LocalGroups } from '../../../components/LocalGroups';
import { CityBanner } from '../../../components/CityBanner';
import { GdanskFAQ } from './_FAQ';
import { getOrganizersByCity } from '../../../data/organizers';
import { EventSection } from '../../../components/EventSection';

export const metadata = {
	metadataBase: new URL(`${env.SITE_URL}/gdansk`),
	title: 'meet.js Gdańsk',
	description: 'JavaScript meetups in Gdańsk',
	keywords: 'meet.js, Gdańsk, JavaScript, meetup',
	openGraph: {
		title: 'meet.js Gdańsk',
		description: 'JavaScript meetups in Gdańsk',
		type: 'website',
		locale: 'en_US',
		url: `${env.SITE_URL}/gdansk`,
		siteName: 'meet.js',
		images: [
			{
				url: `${env.SITE_URL}/api/og?city=Gdansk`,
				width: 1200,
				height: 630,
				alt: 'meet.js | JavaScript meetups in Gdańsk',
			},
		],
	},
	twitter: {
		card: 'summary_large_image',
		site: '@meetjs',
		title: 'meet.js',
		description: 'JavaScript meetups in Gdańsk',
		images: [
			{
				url: `${env.SITE_URL}/api/og?city=Gdansk`,
				width: 1200,
				height: 630,
				alt: 'meet.js | JavaScript meetups in Gdańsk',
			},
		],
	},
};

<CityBanner city="Gdańsk" background="city/gdansk.png" />

<EventSection city="Gdańsk" />

<LocalGroups localGroups={[]} />

<FAQ questions={GdanskFAQ} />

<Organizers city="Gdańsk" organizers={getOrganizersByCity('gdansk')} />

<CityEmail email="gdansk@meetjs.pl" />
