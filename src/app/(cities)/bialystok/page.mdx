import { env } from '../../../env';
import { CityEmail } from '../../../components/CityEmail';
import { Organizers } from '../../../components/Orginizers';
import { FAQ } from '../../../components/FAQ';
import { CityBanner } from '../../../components/CityBanner';
import { BialystokFAQ } from './_FAQ';
import { getOrganizersByCity } from '../../../data/organizers';
import { EventSection } from '../../../components/EventSection';

export const metadata = {
	metadataBase: new URL(`${env.SITE_URL}/bialystok`),
	title: 'meet.js Białystok',
	description: 'JavaScript meetups in Białystok',
	keywords: 'meet.js, Białystok, JavaScript, meetup',
	openGraph: {
		title: 'meet.js Białystok',
		description: 'JavaScript meetups in Białystok',
		type: 'website',
		locale: 'en_US',
		url: `${env.SITE_URL}/city/bialystok`,
		siteName: 'meet.js',
		images: [
			{
				url: `${env.SITE_URL}/api/og?city=Białystok`,
				width: 1200,
				height: 630,
				alt: 'meet.js | JavaScript meetups in Białystok',
			},
		],
	},
	twitter: {
		card: 'summary_large_image',
		site: '@meetjs',
		title: 'meet.js',
		description: 'JavaScript meetups in Białystok',
		images: [
			{
				url: `${env.SITE_URL}/api/og?city=Białystok`,
				width: 1200,
				height: 630,
				alt: 'meet.js | JavaScript meetups in Białystok',
			},
		],
	},
};

<CityBanner city="Białystok" background="city/bialystok.jpg" />

<EventSection city="Białystok" />

<FAQ questions={BialystokFAQ} />

<Organizers city="Białystok" organizers={getOrganizersByCity('bialystok')} />

<CityEmail email="hello@meetjs.bialystok.pl" />
