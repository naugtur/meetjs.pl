import { env } from '../../../env';
import { CityEmail } from '../../../components/CityEmail';
import { Organizers } from '../../../components/Orginizers';
import { FAQ } from '../../../components/FAQ';
import { LocalGroups } from '../../../components/LocalGroups';
import { CityBanner } from '../../../components/CityBanner';
import { WarsawFAQ } from './_FAQ';
import { WarsawOrganizers } from './_Organizers';
import { EventSection } from '../../../components/EventSection';

export const metadata = {
	metadataBase: new URL(`${env.SITE_URL}/warsaw`),
	title: 'meet.js Warsaw',
	description: 'JavaScript meetups in Warsaw',
	keywords: 'meet.js, Warsaw, JavaScript, meetup',
	openGraph: {
		title: 'meet.js Warsaw',
		description: 'JavaScript meetups in Warsaw',
		type: 'website',
		locale: 'en_US',
		url: `${env.SITE_URL}/warsaw`,
		siteName: 'meet.js',
		images: [
			{
				url: `${env.SITE_URL}/api/og?city=Warsaw`,
				width: 1200,
				height: 630,
				alt: 'meet.js | JavaScript meetups in Warsaw',
			},
		],
	},
	twitter: {
		card: 'summary_large_image',
		site: '@meetjs',
		title: 'meet.js',
		description: 'JavaScript meetups in Warsaw',
		images: [
			{
				url: `${env.SITE_URL}/api/og?city=Warsaw`,
				width: 1200,
				height: 630,
				alt: 'meet.js | JavaScript meetups in Warsaw',
			},
		],
	},
};

<CityBanner city="Warsaw" background="city/warsaw.jpg" />

<EventSection city="Warsaw" />

<LocalGroups
	localGroups={[
		'https://www.meetup.com/meet-js-warsaw/',
		'https://lu.ma/user/usr-eNzoaGXYjizOBQI',
	]}
/>

<FAQ questions={WarsawFAQ} />

<Organizers city="Warsaw" organizers={WarsawOrganizers} />

<CityEmail email="warszawa@meetjs.pl" />
